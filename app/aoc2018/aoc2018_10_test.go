package aoc2018

import (
	"fmt"
	"testing"
)

func Test_AOC2018_10_Part1(t *testing.T) {

	lowerbound := -10
	upperbound := 14

	initial := 3
	change_by := -2

	value = applyrange(initial, change_by, lowerbound, upperbound)
	fmt.Printf("[%v] previous %v current %v changeby %v (lowerbound %v upperbound %v)\n", previous, current, changeby, lowerbound, upperbound)

	value = applyrange(initial, change_by, lowerbound, upperbound)
	fmt.Printf("[%v] previous %v current %v changeby %v (lowerbound %v upperbound %v)\n", previous, current, changeby, lowerbound, upperbound)

	value = applyrange(initial, change_by, lowerbound, upperbound)
	fmt.Printf("[%v] previous %v current %v changeby %v (lowerbound %v upperbound %v)\n", previous, current, changeby, lowerbound, upperbound)

	value = applyrange(initial, change_by, lowerbound, upperbound)
	fmt.Printf("[%v] previous %v current %v changeby %v (lowerbound %v upperbound %v)\n", previous, current, changeby, lowerbound, upperbound)

	value = applyrange(initial, change_by, lowerbound, upperbound)
	fmt.Printf("[%v] previous %v current %v changeby %v (lowerbound %v upperbound %v)\n", previous, current, changeby, lowerbound, upperbound)

	value = applyrange(initial, change_by, lowerbound, upperbound)
	fmt.Printf("[%v] previous %v current %v changeby %v (lowerbound %v upperbound %v)\n", previous, current, changeby, lowerbound, upperbound)

}

func Test_AOC2018_10_Part2(t *testing.T) {
}
